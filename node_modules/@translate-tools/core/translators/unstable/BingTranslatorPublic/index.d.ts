import { langCode, langCodeWithAuto } from '../../../types/Translator';
import { BaseTranslator } from '../../../util/BaseTranslator';
/**
 * This translator is very slow for translate page, but may use to translate user input
 */
export declare class BingTranslatorPublic extends BaseTranslator {
    static readonly translatorName = "BingTranslator (public)";
    static isSupportedAutoFrom(): boolean;
    static getSupportedLanguages(): langCode[];
    getLengthLimit(): number;
    getRequestsTimeout(): number;
    checkLimitExceeding(text: string | string[]): number;
    private encodeObject;
    private findInObj;
    translate(text: string, from: langCodeWithAuto, to: langCode): Promise<string>;
    private readonly mtp;
    translateBatch(text: string[], langFrom: langCodeWithAuto, langTo: langCode): Promise<(string | null)[]>;
}
