import { langCode, langCodeWithAuto } from '../../types/Translator';
import { ICache } from '../../types/Cache';
import { IScheduler, ITranslateOptions } from './IScheduler';
import { Scheduler } from './Scheduler';
export declare class SchedulerWithCache implements IScheduler {
    private readonly scheduler;
    private readonly cache;
    constructor(scheduler: Scheduler, cache: ICache);
    translate(text: string, from: langCodeWithAuto, to: langCode, options?: ITranslateOptions): Promise<string>;
    private readonly notLetterRegExp;
    private readonly trimmerRegExp;
    private getTrimmed;
}
